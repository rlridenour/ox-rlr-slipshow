---
theme: vanier
dimension: 16:9
css: ../css/arg.css
js: ../css/speaker-view.js
---

{slip .title-slip}
-----
# Prime Numbers

{.author}
Randy Ridenour

{.date}
2026-07-30


{slip #intro speaker-note=org-slipshow-note-516}
-----
# What is a prime number?

{#org-slipshow-note-516}
> Open by asking who remembers the sieve of Eratosthenes.
>
> Do **not** get drawn into the twin prime conjecture here &mdash; it comes up
> again on the last slip.

Some motivating text before the definition.

{pause}

{.definition title="Prime number"}
A **prime number** is an integer divisible by exactly two integers: 1, and
itself.

Literal braces like \{this one\} must survive the trip intact.

## A nested heading

Inline math $e^{i\pi} = -1$, and display math:

$$\left( \sum_{k=1}^n a_k b_k \right)^2 < \Phi$$

{slip}
-----
# Properties

-   Every integer greater than 1 has a prime factor.
-   [There are infinitely many primes.]{pause}
-   [Primes thin out, but they never stop.]{pause}
-   [See [OEIS A000040](https://oeis.org/A000040) for the sequence itself.]{pause}

{.theorem title="Euclid"}
> There are infinitely many prime numbers.

{.proof}
> Suppose there were finitely many. Multiply them all together and add one.

{speaker-note pause}
> A second note in one section takes a step of its own, so the note can
> change partway through. Give them a moment with the proof first.

{slip}
-----
# An argument in standard form

The last item of a `.arg` list is the conclusion: it loses its number and
gains the inference rule above it.

{.arg}
1.  Every prime greater than two is odd.
2.  Fifty-eight is even, and greater than two.
3.  Fifty-eight is not prime.

{slip}
-----
# Code and quotations

{pause}
```ocaml
let rec fact n = if n <= 1 then 1 else n * fact (n - 1)
```

{blockquote}
> Mathematics is the queen of the sciences, and number theory is the queen
> of mathematics.

{slip}
-----
# Sieving

```=mermaid
graph LR;
    A[Take the next number] --> B[Mark its multiples];
    B --> A;
```

{slip}
-----
# Figures

The attributes below land on the image itself, not on the paragraph
wrapped around it, so the width actually takes effect.

![img](figures/number-line.svg){#number-line style="width:80%"}

The axis is drawn from the start. The primes are a group inside the SVG
carrying `class`"unrevealed"=, so they wait for their own step.

{reveal=primes}

{slip}
-----
# A part from another file

{include src="parts/history.slp"}

{slip}
-----
# Layout

{.columns style="display:flex; gap:2em" children:style="flex:1"}
> > **Left column**
> >
> > Text on the left.
>
> > **Right column**
> >
> > Text on the right.

<hr>

After the horizontal rule. {pause}

An emphasised aside follows.

{slip}
-----
# Data

| Prime | Index |
| ---: | ---: |
| 2 | 1 |
| 3 | 2 |
| 5 | 3 |

