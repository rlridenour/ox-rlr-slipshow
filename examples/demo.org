#+TITLE: Prime Numbers
#+AUTHOR: Randy Ridenour
#+DATE: 2026-07-30
#+OPTIONS: toc:nil
#+SLIPSHOW_THEME: vanier
#+SLIPSHOW_DIMENSION: 16:9

* What is a prime number?
:PROPERTIES:
:ATTR_SLIPSHOW: #intro
:END:

#+begin_notes
Open by asking who remembers the sieve of Eratosthenes.

Do *not* get drawn into the twin prime conjecture here --- it comes up
again on the last slip.
#+end_notes

Some motivating text before the definition.

#+SLIPSHOW: pause

#+ATTR_SLIPSHOW: .definition title="Prime number"
A *prime number* is an integer divisible by exactly two integers: 1, and
itself.

Literal braces like {this one} must survive the trip intact.

** A nested heading

Inline math $e^{i\pi} = -1$, and display math:

\[ \left( \sum_{k=1}^n a_k b_k \right)^2 < \Phi \]

* Properties

#+ATTR_SLIPSHOW: pause-children
- Every integer greater than 1 has a prime factor.
- There are infinitely many primes.
- Primes thin out, but they never stop.
- See [[https://oeis.org/A000040][OEIS A000040]] for the sequence itself.

#+ATTR_SLIPSHOW: title="Euclid"
#+begin_theorem
There are infinitely many prime numbers.
#+end_theorem

#+begin_proof
Suppose there were finitely many. Multiply them all together and add one.
#+end_proof

#+ATTR_SLIPSHOW: pause
#+begin_notes
A second note in one section takes a step of its own, so the note can
change partway through. Give them a moment with the proof first.
#+end_notes

* Code and quotations

#+ATTR_SLIPSHOW: pause
#+begin_src ocaml
let rec fact n = if n <= 1 then 1 else n * fact (n - 1)
#+end_src

#+begin_quote
Mathematics is the queen of the sciences, and number theory is the queen
of mathematics.
#+end_quote

* Sieving

#+begin_src mermaid
graph LR;
    A[Take the next number] --> B[Mark its multiples];
    B --> A;
#+end_src

* Figures

The attributes below land on the image itself, not on the paragraph
wrapped around it, so the width actually takes effect.

#+ATTR_SLIPSHOW: #number-line style="width:80%"
[[file:figures/number-line.svg]]

The axis is drawn from the start. The primes are a group inside the SVG
carrying =class="unrevealed"=, so they wait for their own step.

# Slipshow warns "No element with id 'primes' was found" here: its
# compile-time check does not look inside SVG assets.  The id is present
# in the compiled page and the reveal works.
#+SLIPSHOW: reveal=primes

* A part from another file

#+SLIPSHOW_INCLUDE: parts/history.slp

* Layout

#+begin_columns
#+begin_group
*Left column*

Text on the left.
#+end_group

#+begin_group
*Right column*

Text on the right.
#+end_group
#+end_columns

-----

After the horizontal rule. @@slipshow:{pause}@@

An emphasised aside follows.

* Data

| Prime | Index |
|-------+-------|
|     2 |     1 |
|     3 |     2 |
|     5 |     3 |
